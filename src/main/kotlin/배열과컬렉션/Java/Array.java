package 배열과컬렉션.Java;

public class Array {


    public static void doArray() {
        int[] array = {100, 200};

        for (int i = 0; i < array.length; i++) {
            System.out.printf("%s %s", i, array[i]);
        }
    }
}
